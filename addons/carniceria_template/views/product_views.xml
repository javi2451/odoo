<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_product_template_form_inherit_carniceria" model="ir.ui.view">
        <field name="name">product.template.form.inherit.carniceria</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='general_information']" position="after">
                <page string="Datos Carnicería" name="carnes">
                    <group>
                        <group name="corte_config">
                            <field name="es_corte_carne" widget="boolean_toggle"/>
                            <field name="tipo_corte" invisible="es_corte_carne == False"/>
                            <field name="dias_maduracion" invisible="tipo_corte not in ['primario', 'secundario']"/>
                        </group>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
